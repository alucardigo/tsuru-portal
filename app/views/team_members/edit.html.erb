<% content_for :title, "Editar pesquisador" %>
<% content_for :nav_active, "elegibilidade" %>
<% content_for :crumb, "#{@demand.codigo_display} · Lei do Bem · Equipe" %>

<div class="max-w-[720px] mx-auto px-7 py-6">
  <div class="mb-4">
    <div class="text-[20px] font-semibold tracking-tight text-gray-900">Editar pesquisador</div>
  </div>
  <div class="tsuru-card p-5">
    <%= render "form", member: @member, demand: @demand %>
  </div>
</div>
