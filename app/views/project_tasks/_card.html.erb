<% priority_class = {
  "baixa"   => "bg-gray-100 text-gray-700",
  "media"   => "bg-blue-100 text-blue-700",
  "alta"    => "bg-orange-100 text-orange-700",
  "urgente" => "bg-red-100 text-red-700"
}[task.priority] %>
<% running = task.running_entry_for(current_user) %>
<% tracking = task.tracking_resumo %>

<div class="bg-white rounded-lg border border-gray-200 hover:border-gray-300 hover:shadow-sm p-3 cursor-grab transition"
     data-task-id="<%= task.id %>"
     data-kanban-card
     data-move-url="<%= move_demand_task_path(task.demand_id, task) %>">
  <div class="flex items-start justify-between gap-2 mb-2">
    <div class="flex items-center gap-1.5">
      <input type="checkbox" value="<%= task.id %>"
             data-bulk-select-target="box"
             data-action="change->bulk-select#refresh"
             onclick="event.stopPropagation()"
             class="accent-indigo-600 cursor-pointer">
      <span class="text-[11px] px-2 py-0.5 rounded-full font-medium <%= priority_class %>">
        <%= task.priority_label %>
      </span>
    </div>
    <% if task.atrasada? %>
      <span class="text-[11px] px-2 py-0.5 rounded-full bg-red-50 text-red-700 font-medium">
        ⚠ <%= task.due_date.strftime("%d/%m") %>
      </span>
    <% elsif task.due_date.present? %>
      <span class="text-[11px] text-gray-500"><%= task.due_date.strftime("%d/%m") %></span>
    <% end %>
  </div>

  <%= link_to edit_demand_task_path(task.demand, task), class: "block text-[13px] font-medium text-gray-900 leading-snug mb-2",
        data: { turbo_frame: "modal" } do %>
    <%= task.title %>
  <% end %>

  <% if task.description.present? %>
    <div class="text-[12px] text-gray-500 mb-2 line-clamp-2"><%= task.description.to_s.truncate(80) %></div>
  <% end %>

  <div class="flex items-center justify-between text-[11px] text-gray-500">
    <div>
      <% if task.assignee %>
        <span class="inline-block w-5 h-5 rounded-full bg-indigo-100 text-indigo-700 text-center leading-5 font-semibold mr-1"><%= task.assignee.name.to_s[0, 2].upcase %></span>
        <%= task.assignee.name %>
      <% else %>
        <span class="text-gray-400 italic">sem responsável</span>
      <% end %>
    </div>
    <% if task.horas_resumo %>
      <span class="text-gray-600 font-medium"><%= task.horas_resumo %></span>
    <% end %>
  </div>

  <% if tracking %>
    <div class="mt-1.5 text-[10.5px] text-gray-500 truncate" title="Tempo cronometrado x decorrido desde atribuição">
      <%= tracking %>
    </div>
  <% end %>

  <div class="flex items-center justify-between mt-2 pt-2 border-t border-gray-100">
    <% if running %>
      <%= button_to stop_timer_demand_task_path(task.demand, task), method: :post,
            class: "inline-flex items-center gap-1 text-[11px] px-2 py-1 rounded bg-rose-50 text-rose-700 hover:bg-rose-100 font-medium",
            form: { class: "inline" } do %>
        ⏸ <span data-controller="ticker" data-ticker-start-value="<%= running.started_at.utc.iso8601 %>" class="tabular-nums">00:00:00</span>
      <% end %>
    <% else %>
      <%= button_to start_timer_demand_task_path(task.demand, task), method: :post,
            class: "inline-flex items-center gap-1 text-[11px] px-2 py-1 rounded bg-emerald-50 text-emerald-700 hover:bg-emerald-100 font-medium",
            form: { class: "inline" } do %>
        ▶ Iniciar
      <% end %>
    <% end %>
    <%= button_to demand_task_path(task.demand, task), method: :delete,
          data: { turbo_confirm: "Excluir esta tarefa?" },
          class: "text-[11px] text-gray-400 hover:text-red-600",
          form: { class: "inline" } do %>✕<% end %>
  </div>
</div>
